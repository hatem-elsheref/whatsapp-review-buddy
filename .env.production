# Production build for Laravel on the same host (e.g. https://cs.techsup.tech).
# API is same-origin under /api — no CORS issues.
VITE_APP_NAME="ISNAAD CS"
VITE_DEVELOPER_NAME=Developer
VITE_API_URL=/api
VITE_PUSHER_KEY=
VITE_PUSHER_CLUSTER=mt1
